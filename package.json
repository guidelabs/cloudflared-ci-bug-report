{
	"name": "gl-platform",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"test": "vitest --run --no-file-parallelism --max-workers=1"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.14.1",
		"vitest": "^4.1.2",
		"wrangler": "^4.80.0"
	}
}
